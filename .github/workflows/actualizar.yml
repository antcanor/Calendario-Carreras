name: Actualizar Carreras Diariamente

# Se ejecuta cada día a las 06:00 AM UTC
on:
  schedule:
    - cron: '0 6 * * *'
  # También permite ejecutarlo manualmente desde la web de GitHub
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    # Solo necesitamos leer el código, no escribir
    permissions:
      contents: read

    steps:
      - name: Descargar código del repositorio
        uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar librerías
        run: |
          pip install -r requirements.txt

      - name: Ejecutar Scrapers y Fusión
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python main.py

      - name: 2. Publicar en Instagram
        env:
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python instagram.py

      # Ya no hace falta guardar carreras.db porque todo está en Supabase
#      - name: Guardar cambios (Commit & Push)
#        run: |
#          git config --global user.name 'Actualizador Automatico'
#          git config --global user.email 'bot@noreply.github.com'
#          git add carreras.db
#          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Actualización automática de carreras" && git push)
